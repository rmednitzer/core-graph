{
  "description": "Security events involving a principal since a given ISO-8601 timestamp",
  "parameters": {
    "principal_id": {
      "type": "string",
      "required": true,
      "description": "Keycloak user ID or service account ID"
    },
    "time_threshold": {
      "type": "string",
      "required": true,
      "description": "ISO-8601 UTC timestamp; events at or before this are excluded. Computed by the skill from a hours_back window."
    }
  },
  "returns": "list",
  "max_depth": 2,
  "estimated_ms": 150
}
