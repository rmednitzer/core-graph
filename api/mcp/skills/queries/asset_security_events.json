{
  "description": "Recent security events for an asset since a given ISO-8601 timestamp",
  "parameters": {
    "canonical_key": {
      "type": "string",
      "required": true,
      "description": "SHA-256 canonical key of the host entity"
    },
    "time_threshold": {
      "type": "string",
      "required": true,
      "description": "ISO-8601 UTC timestamp; events at or before this are excluded. Computed by the skill from a hours_back window."
    }
  },
  "returns": "list",
  "max_depth": 3,
  "estimated_ms": 150
}
